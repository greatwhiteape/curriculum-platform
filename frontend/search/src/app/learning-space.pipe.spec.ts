import { LearningSpacePipe } from './learning-space.pipe';

describe('LearningSpacePipe', () => {
  it('create an instance', () => {
    const pipe = new LearningSpacePipe();
    expect(pipe).toBeTruthy();
  });
});
