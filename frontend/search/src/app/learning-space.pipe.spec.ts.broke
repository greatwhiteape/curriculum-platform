import { LearningSpacesPipe } from './learning-spaces.pipe';

describe('LearningSpacesPipe', () => {
  it('create an instance', () => {
    const pipe = new LearningSpacePipe();
    expect(pipe).toBeTruthy();
  });
});
